source "https://rubygems.org"

# Mirrors GitHub Pages' native build environment so local preview matches what
# ships. GitHub Pages builds the site server-side from this gem on every push.
gem "github-pages", group: :jekyll_plugins
